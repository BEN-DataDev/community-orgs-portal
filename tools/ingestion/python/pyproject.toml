[project]
name = "community-orgs-ingestion"
version = "0.1.0"
description = "Offline acquisition prototype; no publication or live transport"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
